# streamlitインストール
pip install streamlit

# app.pyのファイルを作成
app.py

# app.pyのコードを実行
streamlit run app.py

# public keyの作成
ssh-keygen -t rsa

# パス
/Users/yama/.ssh/

# public keyの確認
ssh -T git@github.com

git config --global user.name "yama-ac"
git config --global user.email "hikarunba0101@gmail.com"
git config --global --list
git init
git add -A
git status

git remote add origin https://github.com/yama-ac/stremlit_web_app.git

