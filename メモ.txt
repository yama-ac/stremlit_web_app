# streamlitインストール
pip install streamlit

# app.pyのファイルを作成
app.py

# app.pyのコードを実行
streamlit run app.py

# public keyの作成
ssh-keygen -t rsa

# パス
/Users/yama/.ssh/

# public keyの確認
ssh -T git@github.com

git config --global user.name "アカウントID"
git config --global user.email "メールアドレス"
git config --global --list
git init
git add -A
git status

# gitの更新方法
git add -A
git commit -m "commit実行"
git push -u origin main


